**Summary**
- Added a dedicated `prompt_valet/api` package so the control plane can load the watcher/tree-builder config, expose TreeBuilder discovery, parse `runs/*/job.json` safely, and surface the requested `/api/v1/…` endpoints, all behind a FastAPI app (`prompt_valet/api/config.py:1`, `prompt_valet/api/discovery.py:1`, `prompt_valet/api/jobs.py:1`, `prompt_valet/api/app.py:1`).
- Provided a simple CLI entry (`scripts/pv_api.py:1`) plus an operator sheet (`docs/p1-c3-api.md:1`) describing how to install FastAPI/Uvicorn, run the service, and hit `/healthz`, `/targets`, and `/jobs`.
- Captured the Block A reasoning in `docs/analysis/p1-c3-api-skeleton.md:1`, added regression tests covering discovery/job parsing (`tests/test_p1_c3_api.py:1`), and marked P1·C3 complete in `PHASE_CHECKLIST.md:5`.

Note: Local verification required installing FastAPI/Uvicorn (`python3 -m pip install fastapi uvicorn`) because the repo currently lacks a dependency manifest.

**Testing**
- `ruff check .`
- `black --check .`
- `pytest -q`

**Next Steps**
1. Wire `scripts/pv_api.py` into the deployment (systemd unit, container, etc.) so the API can be reached alongside the watcher.
2. Consider adding dependency management (requirements/pyproject) when ready so FastAPI/Uvicorn can be tracked formally.