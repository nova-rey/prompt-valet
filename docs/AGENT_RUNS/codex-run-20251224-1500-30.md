- Responsive dashboard/detail updates: the job dialog now fits within `max-height: calc(100vh - 120px)`, exposes a “Copy ID” tap, stacks the log controls on phones, and wraps the SSE badge while the job table sits inside an overflow container with a min-width so rows stay touch-friendly (`prompt_valet/ui/app.py:532`, `prompt_valet/ui/app.py:693`).
- Submit and services panels stack selects, CTAs, and badges vertically on small screens, add wrapping/result copy affordances, and keep the refresh/connectivity controls full-width to avoid millimeter-wide targets (`prompt_valet/ui/app.py:774`, `prompt_valet/ui/app.py:1099`).
- Documented the mobile-risk analysis, tracked the hardening checklist, and marked P2·C6 complete so the phase tracker reflects the new work (`docs/analysis/p2-c6-mobile-hardening.md:1`, `PHASE_CHECKLIST.md:14`).
- Tests: `pytest -q`, `ruff check .`, `black --check .` (all green after formatting the touched UI module).
- No backend code was changed; all edits live in the NiceGUI UI layer.
- UI remains API-driven only, continuing to rely on the existing `PromptValetAPIClient`/SSE helpers for every dashboard, submit, and services interaction (`prompt_valet/ui/app.py:693`).

Next steps: manually exercise the NiceGUI surface on a real phone or emulator to ensure the new tap targets, scroll behavior, and SSE labels feel right in a mobile browser.