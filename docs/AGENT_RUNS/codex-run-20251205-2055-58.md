Both scripts now use `/srv/prompt-valet/config/prompt-valet.yaml` for runtime settings, emit the requested `[prompt-valet] ...` startup line with merged watcher info, and no longer depend on TOML at all; the renamed sample config at `configs/prompt-valet.yaml` mirrors the previous structure, and the docs/AGENT notes all reference the new name and path.

- Tests: `python3 -m py_compile scripts/codex_watcher.py`; `python3 -m py_compile scripts/rebuild_inbox_tree.py`.

**Summary**
- Files modified: `scripts/codex_watcher.py`, `scripts/rebuild_inbox_tree.py`, `configs/prompt-valet.yaml` (metadata updated and renamed from `configs/codex-runner.yaml`), `docs/AGENT_RUNS/codex-run-20251205-1216-3291.md`, `docs/Architecture.md`, `docs/Bible.md`, `docs/Config_Spec.md`, `docs/Filesystem.md`, `docs/Phase_Roadmap.md`, `docs/Prompt_Valet_Overview.md`.
- Canonical runtime config path/filename: `/srv/prompt-valet/config/prompt-valet.yaml`.
- Log-line format in `scripts/codex_watcher.py`: `[prompt-valet] loaded config=<path> inbox=<inbox_dir> processed=<processed_dir> git_owner=<owner> git_host=<host> git_protocol=<proto> runner=codex exec`.
- Log-line format in `scripts/rebuild_inbox_tree.py`: `[prompt-valet] loaded config=<path> inbox=<inbox_dir> processed=<n/a> git_owner=<owner> git_host=<host> git_protocol=<proto> runner=<none>`.
- Confirmation: TOML is no longer used as a runtime config source, and both scripts now rely solely on the unified YAML config at `/srv/prompt-valet/config/prompt-valet.yaml`.