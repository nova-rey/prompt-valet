Aligned the updated tests with the current config schema and Git preflight behavior, and ensured the Git sync helper can commit under CI.

- `tests/test_basic_ci.py::test_example_config_yaml_parses_and_has_expected_sections` now accepts either a nested `file_server` or flat `inbox/processed` keys and still enforces `watcher`/`tree_builder` plus runner metadata (either a `runner` section or `runner_cmd`/`runner_model` entries in `watcher`) so the schema assertions match the distributed config (`tests/test_basic_ci.py:71`).
- `tests/test_git_preflight.py::test_ensure_worker_repo_clean_and_synced_*` now expects the revised command order (`git status --porcelain`, `git checkout main`, `git pull --ff-only`), stubs `ensure_repo_cloned` to avoid actual cloning, and its failure case looks for `git status` logging (`tests/test_git_preflight.py:12` and `tests/test_git_preflight.py:25`).
- `tests/test_git_sync.py::_create_repo_with_origin` now configures a local Git user/email before the initial commit to keep the CI-friendly test from failing (`tests/test_git_sync.py:20`).
- `pytest -q` passes locally after these updates.

Next step: push the updates and let CI rerun the suite to verify the changes on the shared runner.