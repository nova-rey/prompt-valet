Hooked repo/branch select updates behind an explicit suppression guard so refresh-driven option/value changes no longer re-enter the change handlers and reset state.

- **Guarded updates** `prompt_valet/ui/app.py:969-1006`: introduced `select_events_suppressed`, `_execute_with_select_suppressed`, and wrapped every programmatic update to `repo_select`/`branch_select` options and values so they run with a suppression flag.
- **Handler early exits** `prompt_valet/ui/app.py:1059-1074`: the repo/branch change handlers now short-circuit when suppression is active, so only user-initiated changes mutate `selected_repo`/`selected_branch`.
- **Testing** Not run (not requested).

Next steps:
1. Manually exercise the Submit panel per the acceptance criteria (switch repos/branches, refresh targets, ensure selections remain stable) to confirm the suppression guard behaves as expected.