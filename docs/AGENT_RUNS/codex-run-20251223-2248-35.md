Delivered the NiceGUI UI skeleton plus the supporting client/settings, CLI, docs, and tracker updates that this checkpoint requires.

**Summary**
- Built the NiceGUI shell with a health-indicator header, responsive Dashboard/Submit/Services tabs, and placeholder cards that explicitly avoid touching job files (`prompt_valet/ui/app.py:1`).
- Added an async `/api/v1/healthz` client plus env-driven settings, wired the CLI entrypoint, and bumped dependencies so the UI can run standalone (`prompt_valet/ui/client.py:1`, `prompt_valet/ui/settings.py:1`, `scripts/pv_ui.py:1`, `pyproject.toml:10`).
- Captured the Block A analysis/plan and documented the UI service plus Phase 2 tracker so operators know how to run the new UI (`docs/analysis/p2-c1-ui-skeleton-analysis.md:1`, `docs/ops/phase1-operator-guide.md:88`, `PHASE_CHECKLIST.md:1`).

**Files touched**
- `prompt_valet/ui/app.py:1`
- `prompt_valet/ui/client.py:1`
- `prompt_valet/ui/settings.py:1`
- `scripts/pv_ui.py:1`
- `pyproject.toml:10`
- `docs/analysis/p2-c1-ui-skeleton-analysis.md:1`
- `docs/ops/phase1-operator-guide.md:88`
- `PHASE_CHECKLIST.md:1`

**Commands run + results**
- `pip install -e ".[dev]"` (installed the editable package plus `nicegui`/`httpx` and their deps)
- `pytest -q` (63 passed)
- `ruff check .` (All checks passed!)
- `black --check .` (All done, 32 files would be left unchanged)

**Known limitations**
- The Submit/Services tabs remain static placeholders with only explanatory text for now (`prompt_valet/ui/app.py:36`); wiring job listings/submissions is future work.
- The UI only polls `/api/v1/healthz` today, so real job/job log interactions still need to be implemented in a follow-up checkpoint (`prompt_valet/ui/app.py:36`).