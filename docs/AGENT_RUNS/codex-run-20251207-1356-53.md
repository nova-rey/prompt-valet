Installed automation, docs, and units so the non-interactive installer now owns filesystem prep, config generation, repo cloning, script deployment, and service registration in one shot.

- `install_prompt_valet.sh:1` now bootstraps the base dirs, installs the required packages/pip modules (including Copyparty), clones/updates the Prompt Valet repo, writes the Phase 1 schema config, emits the optional Copyparty YAML, copies the service/unit templates, reloads systemd, and respects `PV_VALIDATE_ONLY=1`.
- The repo now carries the concrete systemd templates referenced by the installer (`prompt-valet-watcher.service:1`, `prompt-valet-tree-builder.service:1`, `prompt-valet-tree-builder.timer:1`, and `copyparty.service:1`), ensuring each unit is delivered unchanged to `/etc/systemd/system/`.
- Documentation reflects the new implementation: `docs/installer_contract.md:1` spells out the installer obligations and env API, `docs/analysis/P2-C2-analysis.md:1` records the Phase 2·C2 analysis, and `docs/phase-roadmap.md:5` marks Block A/B/C complete for P2·C2.

Testing:
- `bash -n install_prompt_valet.sh`
- `systemd-analyze verify prompt-valet-watcher.service prompt-valet-tree-builder.service prompt-valet-tree-builder.timer copyparty.service` (reports only the unrelated `netplan-ovs-cleanup.service` permission warning).

Next steps:
1. Run `install_prompt_valet.sh` on the target machine to populate `/srv/prompt-valet`, deploy the scripts/units, and start the services with your preferred `PV_*` overrides.
2. If needed, monitor `journalctl -u prompt-valet-watcher.service` (and `-u copyparty.service` when enabled) to validate that the watcher/tree builder boot and honor the generated config.