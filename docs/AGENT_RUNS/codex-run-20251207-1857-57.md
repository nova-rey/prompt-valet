Delivered a non-interactive installer that prepares the filesystem, clones the repo, stitches configuration, deploys scripts, writes the services/timer, and respects `PV_VALIDATE_ONLY`.

- Added `install_prompt_valet.sh:1` to encapsulate the full installer workflow (dependency install, directories, git update, config generation, optional Copyparty config, and systemd unit management with dry-run support).
- Added the systemd templates `systemd/prompt-valet-watcher.service:1`, `systemd/prompt-valet-tree-builder.service:1`, `systemd/prompt-valet-tree-builder.timer:1`, and `systemd/copyparty.service:1` so installers can drop them under `/etc/systemd/system/` with the required ExecStart/Restart semantics.
- Rewrote the documentation contract `docs/installer_contract.md:1`, documented Block A findings in `docs/analysis/P2-C2-analysis.md:1`, and refreshed both roadmap views `docs/phase-roadmap.md:1` / `docs/Phase_Roadmap.md:1` to show implementation and verification completion.

- Tests: `bash -n install_prompt_valet.sh`, `systemd-analyze verify systemd/prompt-valet-watcher.service systemd/prompt-valet-tree-builder.service systemd/prompt-valet-tree-builder.timer systemd/copyparty.service` (only the existing `netplan-ovs-cleanup.service` permission warning appeared), and `python3 -m markdown` over each updated Markdown doc.
- Warnings: `systemd-analyze verify` emits `netplan-ovs-cleanup.service: Failed to open … Permission denied`; no installed unit is affected.

- Next steps: 1) Run `PV_VALIDATE_ONLY=1 ./install_prompt_valet.sh` on a target host to review the planned changes; 2) After confirming, run the installer normally to deploy the services and Copyparty stack (ensure the host is running systemd).