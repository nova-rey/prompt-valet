- `scripts/codex_watcher.py:42` now declares `DEFAULT_PV_ROOT`, and `load_config` (`scripts/codex_watcher.py:785`) merges user YAML, resolves/creates the configured pv_root plus inbox/processed/finished/repos/failed subdirectories relative to it, refreshes the config/queue defaults, and logs both the old `[prompt-valet] loaded config=` line and the new `Using pv_root=… config=…` message so runs honor the requested root.
- `scripts/codex_watcher.py:1458` unpacks the `(cfg, pv_root)` tuple in `main`, keeping the rest of the watcher (inbox/processed directories, queue root, etc.) aligned with the resolved pv_root rather than recomputing `/srv/prompt-valet`.
- `tests/test_e2e_dry_run.py:33` now writes `pv_root: "{root}"` into the temporary YAML so the e2e dry-run config explicitly points the watcher at the tmp tree instead of the hard-coded production root.
- `tests/test_codex_watcher_config_root.py:15` introduces a focused unit test that points `DEFAULT_CONFIG_PATH` at a temp config, spies on `Path.mkdir`, and asserts `load_config()` returns the tmp pv_root and never touches `/srv/prompt-valet`.
- Tests: `pytest -q`.