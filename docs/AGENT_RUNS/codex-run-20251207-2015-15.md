PyYAML is now installed inside the script, every config/unit write is logged so dry-run vs. rerun behavior is explicit, and the docs/roadmaps now describe how humans validate the installer for C3.

**Summary**
- `install_prompt_valet.sh:61`, `install_prompt_valet.sh:119`, `install_prompt_valet.sh:204`, and `install_prompt_valet.sh:288` now install `python3-yaml` and log each config, Copyparty, and systemd write/enable step so idempotency and PV_VALIDATE_ONLY can be audited via the logs.
- `docs/analysis/P2-C3-analysis.md:1` captures the Block A findings and planned improvements, while `docs/installer_contract.md:86` spells out the PV_VALIDATE_ONLY dry run, Debian VM install, and `systemd-analyze verify` workflow operators should follow.
- `docs/phase-roadmap.md:15` and `docs/Phase_Roadmap.md:15` record that P2·C3 Blocks A/B/C are complete with the new validation surface and documentation updates.

**Tests**
- `bash -n install_prompt_valet.sh`
- `systemd-analyze verify systemd/*.service systemd/*.timer` (passes; `netplan-ovs-cleanup.service` could not be opened under `/run/systemd/system` because of permission restrictions, but repo units validated successfully)

Next steps:
1. Run `PV_VALIDATE_ONLY=1 ./install_prompt_valet.sh` on the target environment to compare the dry-run log against the real run actions documented in `docs/installer_contract.md`.
2. Execute the installer on a Debian VM and confirm the `/srv/prompt-valet` layout, `/srv/prompt-valet/config/prompt-valet.yaml`, and the systemd units/services match the expectations laid out in the manual validation section.