**Changes**
- Added `_schedule_async` so all NiceGUI-triggered coroutines (live-log streaming, log refresh, abort confirmation, job-detail loads, and submit/upload flows) are started via a one-shot timer instead of `asyncio.create_task` during the sync build phase (`prompt_valet/ui/app.py:38-1091`), preventing “no running event loop” races and keeping `live_stream_task` assignments (prompt_valet/ui/app.py:323-341) safe.
- Locked the runtime stack in `pyproject.toml:10-19` (`fastapi==0.127.0`, `uvicorn==0.40.0`, `httpx==0.28.1`, `nicegui==3.4.1`, `starlette==0.50.0`, `pydantic==2.12.5`), documented the baseline in `docs/ops/phase1-operator-guide.md:3-16`, and expanded `.gitignore:2-12` with `*.bak.*` so runtime artifacts/backups stay out of git.
- Introduced `tests/test_ui_boot.py:1-25` to import `create_ui_app`, verify it augments the NiceGUI router (expecting a `_nicegui/client/…/upload` route), and then restore the original FastAPI router state.

**Verification**
- `python3 -m compileall -q prompt_valet scripts` (passes with no output).
- `pytest -q` → `80 passed in 2.96s`.
- `ruff check .` → `All checks passed!`.
- `black .` → reformatted `scripts/pv_ui.py:1-14` and `prompt_valet/ui/app.py` to stay aligned with the configured style.
- `PYTHONPATH="$(pwd)" python3 scripts/pv_api.py` + `curl -s http://127.0.0.1:8888/api/v1/healthz` → `{"status":"ok","version":"0.1.0"}`.
- `PYTHONPATH="$(pwd)" PV_API_BASE_URL=http://127.0.0.1:8888/api/v1 python3 scripts/pv_ui.py` + `curl -s http://127.0.0.1:8080/ | rg Dashboard` → the UI page renders and exposes the Dashboard/Submit/Services tabs.

Next: consider running an end-to-end job submission (or the queue watcher) against the pinned stack to surface any remaining runtime edge cases.