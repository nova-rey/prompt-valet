**Summary**
- Wired the NiceGUI job detail dialog to show a “Recent Logs” card, manual refresh, live-log controls (Live Logs toggle / Pause + status badge), and an abort-confirmation flow that types “ABORT” before hitting `/api/v1/jobs/{job_id}/abort`; SSE tasks stop when the detail view closes so logs don’t leak between jobs (`prompt_valet/ui/app.py:523-645` covers the UI and control wiring, and `prompt_valet/ui/app.py:261-415` shows the helper logic including SSE backoff and abort handling).
- Extended `PromptValetAPIClient` with reusable helpers for log tailing, SSE streaming, and abort calls plus a transport hook for testing; added `tests/test_ui_client.py` to cover those helpers without requiring `pytest-asyncio` (`prompt_valet/ui/client.py:24-184` and `tests/test_ui_client.py`).
- Recorded the Phase-2·Checkpoint-4 analysis/plan and marked the checklist entry (`docs/analysis/p2-c4-ui-logs-abort.md`, `PHASE_CHECKLIST.md`).

**Testing**
- `pytest -q` (pass)
- `ruff check .` (pass)
- `black --check .` (pass after formatting `prompt_valet/ui/app.py`)

**SSE reconnect behavior**
- `_run_live_stream` maintains a persistent SSE client that appends `data:` lines, updates status text, and retries on transient errors with exponential backoff (0.5s doubling up to 10s), while the Live Logs toggle + Pause/Disconnect buttons update the UI state (`prompt_valet/ui/app.py:294-352`).

**Backend constraints**
- Logs are only served when the job’s log file exists; the SSE stream closes once the job enters a terminal state, so the UI must rely on `/jobs/{job_id}` for final status updates (`prompt_valet/api/app.py:209-226`).
- Abort requests succeed only while the backend reports a `running` state; otherwise FastAPI returns 409, which we surface via the confirmation modal messaging (`prompt_valet/api/app.py:221-245`).

**Files**
- `prompt_valet/ui/app.py:261-645` – new log buffer/SSE helpers, the “Recent Logs” card, manual refresh/live controls, pause/abort buttons, and typed abort confirmation.
- `prompt_valet/ui/client.py:24-184` – centralized HTTP client now supports abort, log tail, SSE streaming, and optional transport injection for testing.
- `tests/test_ui_client.py` – synchronous asyncio-driven tests for the new client helpers using `httpx.MockTransport`.
- `docs/analysis/p2-c4-ui-logs-abort.md` – Block A analysis/contract/plan for logs + abort work.
- `PHASE_CHECKLIST.md` – added the P2·C4 bullet summarizing the new job-detail logs/abort UI.

Next steps
1. Manually exercise the NiceGUI job detail dialog to confirm scroll/selection behavior, SSE status labels, and the abort confirmation flow.
2. (Optional) Deploy/run the UI against a live Phase 1 API to verify log reconnections across network hiccups.