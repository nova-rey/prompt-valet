# Prompt Valet UI environment defaults.
# Copy to /etc/prompt-valet/pv-ui.env and edit before starting prompt-valet-ui.service.

PV_API_BASE_URL=http://127.0.0.1:8888/api/v1   # API root that the UI polls for health, jobs, and services.
PV_UI_BIND_HOST=0.0.0.0                         # Host nicegui.run listens on (UI binding address).
PV_UI_BIND_PORT=8080                             # Port nicegui.run listens on (UI binding port).
PV_UI_API_TIMEOUT_SECONDS=5.0                    # Seconds before UI HTTP calls to the API fail.
PV_UI_USER=prompt-valet                          # OS user to drop to when running the UI service.
