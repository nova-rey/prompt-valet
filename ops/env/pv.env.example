# Prompt Valet API environment defaults.
# Copy to /etc/prompt-valet/pv.env and edit as needed before starting prompt-valet-api.service.

PV_CONFIG_PATH=/srv/prompt-valet/config/prompt-valet.yaml
PV_REPOS_ROOT=/srv/prompt-valet/inbox   # Tree-builder / inbox root (CONFIG["inbox"]).
PV_RUNS_ROOT=/srv/prompt-valet/runs      # Where watcher writes job metadata/logs.
PV_BIND_HOST=127.0.0.1                  # uvicorn bind host for the API.
PV_BIND_PORT=8888                         # uvicorn bind port for the API.
PV_STALL_THRESHOLD_SECONDS=60            # seconds before a running job is reported as stalled.
PV_API_USER=prompt-valet                 # UNIX user to drop to when running the API.
